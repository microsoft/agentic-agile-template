# Wave 6 Adversarial Review Report — Disconnected-Fork Consolidation

> **Companion artifact** to [`2026-06-consolidation.md`](./2026-06-consolidation.md) (Wave 7 retrospective). This file contains the report generated by the `code-review` sub-agent against `consolidation` @ `e4e2a5b` on 2026-06-05.
>
> **Redaction & retention policy for this public-facing copy:**
>
> - **Redacted:** the internal-staging GitHub organization slug and any URLs pointing at the internal staging tracker. The literal internal-org URL that the C1 finding itself flagged has also been dropped (to avoid re-leaking the very identifier the finding warned about), with the surrounding narrative redacted accordingly.
> - **Retained (intentionally):** bare internal-tracking issue numbers (e.g., `#37`, `#57`) and the commit short-SHA `e4e2a5b` (the commit the report was generated against). These are kept so the maintainer team can cross-reference the original findings in the internal staging tracker.
> - **Otherwise preserved:** the 17 findings (1 Critical, 9 High, 4 Medium, 3 Low), their evidence, severity assignments, and recommendation text are preserved from the original report. Per the E6 amendment, Critical/High/triaged Medium findings were fixed pre-merge in a single batch commit; Low findings (L1–L3) were backlogged for follow-up.

---

# Wave 6 Adversarial Review — consolidation @ e4e2a5b

## Summary
- Files reviewed: 51 (full unified-PR delta against `upstream/main`)
- Critical findings: 1
- High findings: 9
- Medium findings: 4
- Low findings: 3

---

## Critical findings (block external PR)

### C1. PII / internal-org leak: internal-org URL in publicly-published sample-issues README
**File:** `docs/sample-issues/README.md:42`
**Severity:** Critical
**Problem:** The "Snapshot provenance" paragraph contains a link to the internal staging repo (URL redacted from this public copy).

This is the exact class of leak Wave 6 was tasked to catch: a stray reference to the internal staging repo that survived redaction. Once the consolidation lands as a public PR on `microsoft/agentic-agile-template`, this URL becomes a public footprint of the internal repo — visible from the docs/ index, indexable by search engines, and a discoverability hook for an org URL that should remain non-public.
**Evidence:** A repository-wide grep for the internal-org identifier returned exactly one hit — this line — confirming it was the only leak and that other redactions (e.g., the prior `codespace-psychic-trout-*` branch name) succeeded.
**Suggested fix:** Replace the parenthetical with a non-leaky framing such as "(see the original consolidation effort tracked internally)" or simply remove the URL; the surrounding sentence still makes sense without it.

---

## High findings (should fix before external PR if cheap)

### H1. `platform-adapters/agent-tools/README.md` "Available Adapters" table is wrong in both directions
**File:** `platform-adapters/agent-tools/README.md:5-13`
**Severity:** High
**Problem:** The Available Adapters table — the primary navigation entry for the entire agent-tools layer — lists two adapters that **don't exist on disk** (`claude-code/`, `amplifier/`) and **omits two that do** (`cline/`, `aider/`).
**Evidence:**
- Disk: `aider/`, `claude/`, `cline/`, `cursor/`, `github-copilot/`, `windsurf/`
- Table: `github-copilot/`, `claude-code/`, `cursor/`, `windsurf/`, `amplifier/`
- Status: `amplifier/ — Community` and `claude-code/ — Community` are unsubstantiated claims about adapters that don't ship.

**Suggested fix:** Replace table rows with the six actual on-disk adapters (`aider`, `claude`, `cline`, `cursor`, `github-copilot`, `windsurf`). Drop `amplifier` and `claude-code` entirely or move to a "Planned" subtable.

### H2. `platform-adapters/agent-tools/README.md` "Three-File Anatomy" contradicts canonical anatomy and disk reality
**File:** `platform-adapters/agent-tools/README.md:21-38`
**Severity:** High
**Problem:** This README describes the three-file anatomy as `README.md` + `context.md` + `mcp.json` (optional). But:
1. CONTRIBUTING-AGENTS.md §"Adapter Anatomy" (lines 123-149) defines it as `README.md` + target-named file + `quickstart.md`.
2. Every on-disk adapter follows the canonical anatomy: `claude/` has `CLAUDE.md` + `quickstart.md`, `cursor/` has `.cursorrules` + `quickstart.md`, etc. None has `context.md` or `mcp.json`.

This is the first thing a contributor reads when navigating to the agent-tools layer. Following its instructions produces the wrong file layout.
**Suggested fix:** Rewrite §"Three-File Anatomy" to mirror the canonical description in CONTRIBUTING-AGENTS.md (README.md + target-named file + quickstart.md).

### H3. `platform-adapters/README.md` claims, diagram, and adapter-creation steps don't match reality
**File:** `platform-adapters/README.md:25-29, 57-60, 81-83`
**Severity:** High
**Problem:** Multiple co-located misstatements:
- Diagram (lines 25-29) lists `Claude Code`, `Amplifier`, `Shortcut` in adapter columns; none of these directories exist.
- "Adding an agent-tools adapter" step 3 (line 59): "Include `context.md`, `mcp.json`, and `instructions.md`" — none of these filenames exist in any actual adapter; contradicts CONTRIBUTING-AGENTS.md and contradicts the adjacent diagram on line 58 which points to `agent-tools/README.md` (which itself contradicts disk per H2).
- "Status at a Glance" combo row (line 83): `[copilot-github-actions](combos/)` — the file is actually `copilot-github-github-actions.md` and the link points to the directory, not the file.

**Suggested fix:** Update the diagram to list real adapters (claude, github-copilot, cursor, windsurf, cline, aider; github only; — for ci-cd); rewrite the "Adding…" steps to match the canonical anatomy; fix the combo link to `[copilot-github-github-actions](combos/copilot-github-github-actions.md)`.

### H4. `platform-adapters/combos/README.md` lists 6 combo filenames; only 3 exist and none of the names match
**File:** `platform-adapters/combos/README.md:13-22, 55-71`
**Severity:** High
**Problem:**
- Available Combos table lists `copilot-github-issues-github-actions.md`, `claude-code-github-issues-github-actions.md`, `cursor-github-issues-github-actions.md`, `amplifier-github-issues-github-actions.md`, `copilot-jira-github-actions.md`, `copilot-linear-github-actions.md`.
- Actual disk: `claude-github-github-actions.md`, `copilot-github-github-actions.md`, `cursor-github-github-actions.md`.
- The naming-convention section (lines 64-71) says the issue-tracker slug is `github-issues`, but the only issue-tracker adapter directory is `github/`, so the prescribed convention immediately breaks against reality.

Users will click "Available Combos" entries and 404. Contributors who follow the convention will produce filenames that don't compose with existing layer slugs.
**Suggested fix:** Replace the table with the three real combo files (with Status set to actual support level — `Stub` is probably accurate for all three since they're 20-line skeletons). Fix the convention's `<issue-tracker>` slug example to `github` (matching the actual adapter slug). Drop or mark-Planned the amplifier/jira/linear rows.

### H5. `platform-adapters/issue-trackers/README.md` "Available Adapters" claims 4 nonexistent adapters with Maintained/Community status
**File:** `platform-adapters/issue-trackers/README.md:5-13`
**Severity:** High
**Problem:** Table lists `github-issues/`, `azure-devops/`, `jira/`, `linear/`, `shortcut/`. Only `github/` exists on disk. The `azure-devops` row claims "Community" status and the `jira` and `linear` rows claim "Community" — but the directories don't exist at all, so the status claims are fictional and the implication of partial maintenance is misleading. Also note the slug mismatch: the table row says `github-issues/` but the on-disk directory is `github/`.
**Suggested fix:** Replace with a single `github/ — Maintained` row, or split into "Available" (`github/`) and "Planned" subtables. Fix the slug to `github/`.

### H6. `.github/contributor-templates/new-adapter-story.md` will direct contributors to create the wrong file in the wrong location
**File:** `.github/contributor-templates/new-adapter-story.md:23-77`
**Severity:** High
**Problem:** The template tells contributors to create:
- `[layer]/[tool-name]/[tool-name].md` — but the canonical "target-named file" rule (CONTRIBUTING-AGENTS.md lines 138-145) requires the file to be named after the *actual target filename in the project* (e.g., `.cursorrules`, `CLAUDE.md`, `copilot-instructions.md`, `CONVENTIONS.md`, `.windsurfrules`). Following the template, a contributor adding a Cursor adapter would create `cursor/cursor.md` instead of `cursor/.cursorrules`.
- The paths also omit the required `platform-adapters/` prefix throughout, so the literal interpretation places files at the repo root instead of under `platform-adapters/`.

This template will produce structurally-noncompliant adapters that violate the §"What Not to Break" invariants the same file is supposed to enforce.
**Suggested fix:** Replace every `[layer]/[tool-name]/[tool-name].md` placeholder with `platform-adapters/[layer]/[tool-name]/<target-filename>` and add a note explaining that `<target-filename>` is the literal filename the agent reads in a downstream project (e.g., `.cursorrules`, `CLAUDE.md`).

### H7. `CONTRIBUTING-AGENTS.md` directory tree still shows `docs/plans/` (renamed to `docs/history/` in Wave 3)
**File:** `CONTRIBUTING-AGENTS.md:70`
**Severity:** High
**Problem:** Line 14 of the same file correctly references `docs/history/2026-06-04-multi-platform-generalization-design.md`, but the structure tree on line 70 still lists `│   └── plans/                         ← design and implementation plans`. Internally inconsistent and the only stale `docs/plans/` reference left in the tree.
**Evidence:** `grep -rn "plans/" --include='*.md'` returns exactly this one hit. `docs/plans/` does not exist on disk.
**Suggested fix:** Change line 70 to `│   └── history/                       ← design and implementation history`.

### H8. Every agent-tools `quickstart.md` mislabels the framework's canonical `AGENTS.md` as "Amplifier adapter"
**Files:**
- `platform-adapters/agent-tools/aider/quickstart.md:141, 154`
- `platform-adapters/agent-tools/claude/quickstart.md:78, 85`
- `platform-adapters/agent-tools/cline/quickstart.md:107, 119`
- `platform-adapters/agent-tools/cursor/quickstart.md:106, 116`
- `platform-adapters/agent-tools/github-copilot/quickstart.md:78, 85`
- `platform-adapters/agent-tools/windsurf/quickstart.md:101, 112`

**Severity:** High
**Problem:** All six "Multiple Agent Tools Compatibility" tables list `AGENTS.md` with the note "Amplifier agent context" or "Amplifier adapter", and the file-layout examples show `AGENTS.md` as Amplifier's file. But this template's own `AGENTS.md` (line 12) declares: *"This file (`AGENTS.md`) is the single canonical entry point for all AI coding agents."* `AGENTS.md` is a tool-agnostic convention (Codex, Aider, Cursor, etc.), not Amplifier-specific.

This systematic mislabeling, repeated six times, contradicts the central organizing principle of the framework and will confuse every downstream user who reads any quickstart. It also undermines AGENTS.md's "universal entry point" positioning that justified the rewrite in the first place.
**Suggested fix:** Replace the Amplifier-attribution rows with a row describing `AGENTS.md` as the universal orchestrator / cross-agent entry point per the `agents.md` convention.

### H9. `platform-adapters/agent-tools/github-copilot/copilot-instructions.md` references a CLAUDE.md that doesn't exist (and violates adapter-independence invariant)
**File:** `platform-adapters/agent-tools/github-copilot/copilot-instructions.md:3`
**Severity:** High
**Problem:** The template's second line says:
> "This file provides GitHub Copilot with project-specific context. For the full development process, see `CLAUDE.md` in the repository root."

Three concrete problems:
1. **Template-repo correctness:** This is the *full* content that `.github/copilot-instructions.md` (the stub) defers to. The template repo no longer has a root `CLAUDE.md` (moved to `platform-adapters/agent-tools/claude/CLAUDE.md` in this PR). Copilot reading the redirect chain gets pointed at a file that isn't there.
2. **Downstream correctness:** Per the github-copilot quickstart, downstream users adopting *only* the github-copilot adapter will copy this file into their repo. They will likewise have no root CLAUDE.md, so the pointer dangles for every github-copilot-only adopter.
3. **Invariant violation:** CONTRIBUTING-AGENTS.md §"What Not to Break" #3 (line 165-167) explicitly forbids agent-tools adapters from referencing other adapters' target files. This line cross-references the claude adapter's target file.

**Suggested fix:** Remove the "see CLAUDE.md" sentence (or replace with "see `AGENTS.md` for the universal agent entry point"). The github-copilot adapter should stand alone.

---

## Medium findings (defer to follow-up issue OK)

### M1. Three-way contradiction on combo file section structure
**Files:**
- `platform-adapters/combos/README.md:80-102` (spec): Prerequisites · Setup · Verification · Known Issues · Changelog
- `.github/contributor-templates/new-combo-story.md:30-33` (contract): Stack · Status · Adapters used · Notes
- `platform-adapters/combos/copilot-github-github-actions.md` (and the two sibling combos, actually shipped): Stack · Status · Adapters Used · Quick Setup · Notes

**Severity:** Medium
**Problem:** Three different sources of truth, none agreeing. A contributor will fail validation no matter which they follow. The shipped combo files don't even have the sections (Prerequisites, Verification, Known Issues, Changelog) that the combos/README claims are required.
**Suggested fix:** Pick one canonical structure (the simpler shipped version is probably the right choice for v1) and align combos/README.md and new-combo-story.md to match.

### M2. `.github/contributor-templates/CONTRIBUTOR_PR_TEMPLATE.md` checklist uses non-existent "system prompt" file terminology
**File:** `.github/contributor-templates/CONTRIBUTOR_PR_TEMPLATE.md:42`
**Severity:** Medium
**Problem:** Checklist item: *"All required adapter files are present (system prompt, README, optional files)"*. The adapter anatomy has no concept of a "system prompt" file; the three required files per CONTRIBUTING-AGENTS.md are README.md + target-named file + quickstart.md. A contributor cannot verify this checklist item against the actual invariants.
**Suggested fix:** Reword to "(README.md, target-named template file, quickstart.md)".

### M3. SAMPLE 2 walkthrough is anchored to the pre-multi-platform CLAUDE.md model
**Files:** `docs/sample-issues/10-getting-started-with-the-agentic-agile-template.md`, `docs/sample-issues/11-customize-agent-context-file-claude-md.md`
**Severity:** Medium
**Problem:** SAMPLE 2 — the canonical "Onboarding worked example" being published as documentation — frames "Customize CLAUDE.md" as the first step of onboarding any project. Post-multi-platform-rewrite, CLAUDE.md is one of six equal adapters and is *not* the canonical onboarding step (that's now AGENTS.md + adapter selection). Reading SAMPLE 2 in 2026 from a downstream project would lead users back to a single-platform mental model the rest of the docs no longer endorse.

Since these are explicitly verbatim snapshots, rewriting them would break the "verbatim" contract. But the docs/sample-issues/README.md does not currently flag that SAMPLE 2 reflects an earlier methodology framing.
**Suggested fix:** Add a one-sentence advisory to `docs/sample-issues/README.md` noting SAMPLE 2 predates the multi-platform rewrite and treats CLAUDE.md as the default adapter, and refer readers to AGENTS.md for the current onboarding flow.

### M4. `.github/contributor-templates/CONTRIBUTOR_PR_TEMPLATE.md` branch-naming convention conflicts with CONTRIBUTING-AGENTS.md
**File:** `.github/contributor-templates/CONTRIBUTOR_PR_TEMPLATE.md:53`
**Severity:** Medium
**Problem:** PR template says branch naming follows `adapter/<name>`, `combo/<name>`, `methodology/<topic>`, `fix/<description>`. CONTRIBUTING-AGENTS.md §"Contribution Workflow" line 184 and §"Commit Message Format" line 282 use the conventional-commit prefix style (`feat/add-windsurf-adapter`). Contributors get conflicting guidance.
**Suggested fix:** Align the PR template branch-naming bullet to the conventional-commit style used elsewhere.

---

## Low findings (informational — likely not worth fixing now)

### L1. `AGENTS.md` references `docs/agent-tools/` and `docs/issue-trackers/` directories that don't exist
**File:** `AGENTS.md:69, 194`
**Severity:** Low
**Problem:** Line 69 says "Find your platform adapter: `platform-adapters/<your-tool>/` or `docs/agent-tools/<your-tool>/`." Line 194 Verification Checklist says "The issue tracker is documented in the adapter or in `docs/issue-trackers/`." Neither `docs/agent-tools/` nor `docs/issue-trackers/` exists. These are dead pointers but the primary path (`platform-adapters/...`) works, so the failure is graceful for most agents.
**Suggested fix:** Drop the `docs/...` alternates, or create stub README files at those paths.

### L2. `2026` dates in `docs/history/` filenames and `docs/sample-issues/` metadata
**Files:** All three `docs/history/2026-06-04-*.md` files; all 10 `docs/sample-issues/*.md` Created timestamps
**Severity:** Low
**Problem:** Dates are in 2026 (e.g., `2026-05-13T01:46:20Z`). If this reflects the actual GitHub API `created_at` from the source issues, it's faithful and should stay. If it reflects a system-clock skew on the machine that generated the snapshots, it's a permanent inaccuracy in public-facing metadata.
**Suggested fix:** Verify against the live issue `created_at` via `gh api`. If inaccurate, regenerate metadata.

### L3. `.github/workflows/codeql.yml` matrix.include is empty — CodeQL never scans anything
**File:** `.github/workflows/codeql.yml:42-53`
**Severity:** Low (pre-existing — unchanged from `upstream/main`)
**Problem:** `strategy.matrix.include:` is followed only by comments; no actual entries. The matrix expands to zero combinations and the `analyze` job is silently skipped, so the security analysis never runs. This is not a consolidation regression — `git diff upstream/main..consolidation -- .github/workflows/codeql.yml` is empty — but it's worth flagging since the template ships a workflow whose name promises CodeQL analysis but produces none.
**Suggested fix:** Out of scope for this PR. Open a separate issue against the template; consider whether to populate the matrix or remove the workflow given there is no application code to scan.

---

## What I checked and did NOT find issues with
- Cross-reference targets in AGENTS.md (`STYLE.md`, `MANIFESTO.md`, `mcp.json`, `docs/agent-surface-selection.md`, `docs/evaluation-framework.md`, `docs/epic-decomposition-example.md`, `.github/copilot-instructions.md`, `.github/ISSUE_TEMPLATE/agentic-story.md`, `docs/history/`, `platform-adapters/`) — all exist.
- `.github/copilot-instructions.md` is a thin stub correctly redirecting to `platform-adapters/agent-tools/github-copilot/copilot-instructions.md`, which is non-empty.
- `platform-adapters/agent-tools/claude/CLAUDE.md` preserves the issues-first rule (lines 134-138) and the SAMPLE-issue guardrail (line 156) that public's #19 was supposed to introduce.
- All 10 `docs/sample-issues/*.md` files have intact metadata tables with the 6 expected fields (Original issue, Sample group, Author, Created, Labels, Comment count).
- All 10 `docs/sample-issues/*.md` files and the index correctly attribute authorship to the cleared `@dgepstein` handle and to no other real-name handle. The only other "@" handles in the tree are `@users.noreply.github.com` (Amplifier bot) and template-style `@param`/`@returns` in STYLE.md.
- The `microsoft/agentic-agile-template#N` references in sample-issue snapshots (`#5`–`#14`, `Part of #5`, `Depends on #6`, etc.) resolve correctly on GitHub when the docs are read from `microsoft/agentic-agile-template`.
- README.md "Use this template" link (line 72) points at `https://github.com/microsoft/agentic-agile-template`, not the internal mirror.
- README.md `Sample Issues` section (lines 59-66) correctly points at `docs/sample-issues/` and the README.md index.
- `docs/history/` files contain no internal-only URLs (`*.microsoft.com` non-public, `aka.ms/*`, `o365.com`, etc.). The only `*.microsoft.com` hits are the public `developer.microsoft.com/blog/`, `opensource.microsoft.com/codeofconduct/`, and `www.microsoft.com/msrc/` URLs — all safe.
- No `codespace-psychic-trout-*` branch-name remnants anywhere in the tree.
- `.github/workflows/codeql.yml` and `.github/workflows/require-copilot-review.yml` are valid YAML.
- `.github/workflows/require-copilot-review.yml` logic is sound for once-public (the noted private-org `copilot-pull-request-reviewer` limitation will go away in public).
- All `.github/contributor-templates/*.md` placeholder syntax is well-formed (no broken `<!-- -->` blocks, no orphaned `[brackets]`).
- `.cursorrules`, `.windsurfrules`, `.clinerules`, `CONVENTIONS.md`, `CLAUDE.md`, `copilot-instructions.md` adapter target files are all non-empty and consistent in shape (research-note comment at top + agent-context template body).
- `platform-adapters/issue-trackers/github/` adapter (README.md + quickstart.md) is internally consistent.
- No broken `[text](path)` markdown links in AGENTS.md, CONTRIBUTING-AGENTS.md, README.md, or any of the top-level layer READMEs (the issues found are in plain-text table cells, not formal markdown links).

---

## Recommendation

**READY TO PROCEED after fixing critical findings.**

Mandatory before external PR:
- **C1** — strip the internal-org URL from `docs/sample-issues/README.md`. Single-line fix, zero risk, and the *only* finding that constitutes a security/privacy regression in publishing to public.

Strongly recommended (cheap, all in one cleanup pass — they're concentrated in the platform-adapters README layer):
- **H1, H2, H4, H5** — bring the `platform-adapters/{README,agent-tools/README,combos/README,issue-trackers/README}.md` tables and anatomy sections in line with what actually ships. Right now these are the layer's front doors and they document a fictional fleet of adapters.
- **H7** — one-character fix in `CONTRIBUTING-AGENTS.md` (`plans/` → `history/`).
- **H8** — six near-identical edits across quickstart files; clear template misdirection.
- **H9** — one-line edit in github-copilot template.
- **H6** — fix `new-adapter-story.md` paths/placeholders so contributors don't break the structural invariants their issue is supposed to enforce.

The Medium and Low findings can be filed as follow-up issues under Wave 6 triage (#57) without blocking the external PR, provided C1 is addressed.